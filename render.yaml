services:
  - type: web
    name: retirement-planner
    env: python
    region: us-west
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd retirals && uvicorn src.main:app --host 0.0.0.0 --port 8080
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
